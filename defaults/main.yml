---
firewall: true
  
extra_packages:
  - htop
  - net-tools

services:
  - name: http
    port: 80
    protocols:
      - tcp
      - udp
  - name: https
    port: 443
    protocols:
      - tcp
      - udp
